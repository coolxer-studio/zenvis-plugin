# 用户事件分析插件（测试）
--- 

## 插件说明

- 这是一个用于测试插件功能的示例插件，它具备了丰富的功能模块。
- 首先，它能够高效地收集用户操作产生的事件日志，确保每一个用户行为都被准确记录下来。
- 接着，这些日志会被规范地存储到数据库中，方便后续的查询和分析。
- 此外，插件还提供了友好的展示界面，用户可以清晰地查看日志的详细信息。
- 同时，它还支持灵活的菜单配置功能，用户可以根据自己的需求自定义菜单项，使得整个插件的操作更加便捷和个性化。
--- 

## 基本信息

- **插件名称**: 用户事件分析插件（测试）
- **包名**: com.coolxer.plugin.user.event
- **版本**: 1.0.0
- **作者**: CoolXer
- **描述**: 用于测试插件功能的一个示例插件，定义了用户操作的事件日志收集、入库、展示、菜单配置等功能
--- 

## 功能介绍

### 插件外部依赖

无
--- 

### 元数据配置

构建了用于测试的元数据信息，包含以下内容：
--- 

#### 实体名称

user-event

#### 数据库说明

表名：`msg_user_event`

| 字段名       | 数据类型       | 描述                     |
|--------------|----------------|--------------------------|
| id           | String         | 事件id，测试事件唯一标识符 |
| procid       | UInt16         | 进程id，进程             |
| user         | String         | 用户名称                 |
| event_type   | String         | 事件类型                 |
| reliability  | Float64        | 可信度，行为的可信评估结果 |
| detail       | json           | 数据详情                 |
| tags         | Array(String)  | 标记                     |
| server_time  | DateTime64(3)  | 入库时间                 |

--- 

### 数据推送服务

本系统主要用于测试验证场景，其核心功能是数据的产生与入库。在具体运行过程中，系统能够自动且随机地生成数据，这一过程是完全自动化的，无需人工干预。为了保证数据的持续性和稳定性，系统采用定时触发机制，每隔5秒便会自动生成一组新的数据。这些数据随后会被准确无误地存储到指定的数据库中，以便后续的查询、分析和验证工作。通过这种方式，系统能够有效地模拟真实场景中的数据产生过程，为测试验证工作提供了可靠的数据支持，从而确保整个测试流程的顺利进行和结果的准确性。

--- 

### API接口

插件提供标准API接口，以JAR包形式提供服务，接口包含：

### 添加用户接口

#### 请求

```bash
curl -X POST http://localhost:8080/plugin/user/add \
-H "Content-Type: application/json" \
-d '{"id": "12345", "name": "John Doe", "card": "1234567890123456"}'
```

#### 响应

```json
{
  "status": 0,
  "msg": "请求成功",
  "data": "添加成功John Doe"
}
```

### 查询用户列表接口

#### 请求

```bash
curl http://localhost:8080/plugin/user/list?page=1&perPage=5
```

#### 响应

```json
{
  "status": 0,
  "msg": "请求成功",
  "data": {
    "rows": [
      {
        "id": "id0",
        "user": "name9",
        "card": "card4"
      },
      {
        "id": "id1",
        "user": "name4",
        "card": "card2"
      }
    ],
    "total": 3
  }
}
```
--- 

### 可视化配置

插件包含可视化页面配置，支持对test实体的增删改查操作。
--- 

### 菜单项

插件提供以下菜单项：

1. 测试调试可视化（配置菜单）
2. 测试调试（单页面菜单）
--- 

## 使用说明

1. 创建插件：登录->系统管理->插件管理->创建插件->上传插件包。
2. 安装插件：插件列表找到已经安装的插件->点击安装
3. 菜单调整：安装完成后自动创建菜单，默认都是一级菜单，可在菜单管理中调整菜单位置。
4. 插件修改：根据业务需求配置相关插件参数即可
5. 插件导出：插件列表找到已经安装的插件->点击导出（包含已经修改后的插件配置文件、元数据文件、API接口文件、可视化页面文件、菜单项文件、插件图标文件）
6. 卸载插件：插件列表找到已经安装的插件->点击卸载
7. 删除插件：插件列表找到已经安装的插件->点击删除
--- 